import { Event } from "@/types/event";

interface Props {
  event: Event;
}

const EventDetailsAbout = ({ event }: Props) => {
  return null; // We moved the content to EventDetailsHeader
};

export default EventDetailsAbout;